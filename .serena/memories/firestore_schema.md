- `ads/{id}` — DUAL: jobs board (`work|service|ad|sell`, status pending|active|completed|blocked) + marketplace (`cheap_product`, active|inactive). Jobs board: owner patch + `isAdmin()` moderation; admin/owner delete via rules + admin CF. cheap_product: owner patch; **`cheapProductAdminPatchOk` / `cheapProductAdminDeleteOk` for admin** + admin CF (`adminDeleteMarketAd` etc.). `complaints/{id}` — create public; read/update resolve admin-only.