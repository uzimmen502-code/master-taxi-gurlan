- `wallet_topup_requests/{id}` — Telegram card top-up (awaiting_transfer|awaiting_review|credited|rejected|…); CF-only write; read: financeReader OR isAdmin OR owner. Admin Хабарлар→Wallet табда қилгунча/қилинган/жами суммалари шу коллекциядан. Receipts in Storage `topup_receipts/{uid}/`.
- `wallet_withdraw_requests/{id}` — app/Telegram withdraw (pending|approved|paid|rejected); app fields: payoutCardNumber, payoutCardHolder, source=app|telegram; CF-only write. Gated by `users.security.deviceTrustMode=limited` (24s after peer device transfer).
- `device_transfer_requests/{id}` — Faza 1 peer transfer (phone, fromHash, toHash, status pending|approved|rejected|expired|cancelled, expiresAt ~30m). CF-only write; read: owner phone or admin. Indexes: phone+createdAt, phone+status.
- `device_bindings` / `device_aliases` — fingerprint↔phone; soft block 30m on wrong-phone attempts.
- `telegram_links/{tgId}`, `telegram_link_codes/{code}`, `telegram_bot_sessions/{tgId}` — bot link/session; CF-only.
- `settings/wallet_bot` — depositCard*, limits, enabled, topUpApproveMode, withdrawApproveMode, withdrawAutoLimit (20k|50k|100k).
- `settings/app.marketAutoApprove` (bool) — Onlayn BOZOR эълонларини авто фаоллаштириш (default false/manual).
- `settings/app.platformFeaturedAuto` (bool) — Платформа «Тавсия этамиз» витринаси (default true=АВТО; false=фақат featuredOnHome).
- `settings/app.jobsAutoApprove` (bool) — ИШ ЭЪЛОН авто-тасдиқ (default false=ҚЎЛДА pending; true=дарҳол active).
- `settings/app.tvAutoApprove` (bool) — TV Market клиплар (default false=ҚЎЛДА pending; true=дарҳол active).
- `tv_clips/{id}` — video commerce clips: videoUrl, posterUrl, title, price (0=hidden), districtId, ownerPhone, ownerName (given name), category product|service, status pending|active|blocked, like/comment/view counts, shopItemId, socialConsent, socialPostedAt, createdAt. Read: true; create: isAuth; update: admin/owner **or** likeCount ±1 only; delete: admin/owner. Subcol `likes/{uid}` create/delete own. Indexes: status+districtId+createdAt DESC; status+districtId+viewCount DESC; ownerPhone+createdAt DESC; status+createdAt DESC. Storage `tv_clips/{ownerPhone}/`.
- `tv_public_profiles/{ownerPhone}` — public publisher name (`name`, `updatedAt`). Read: true; create/update/delete: owner phone match (name 1..80). Hydrate fallback when `tv_clips.ownerName` is empty/@nick/«Фойдаланувчи»; also `tv_shops.name`.
- `tv_shops/{ownerPhone}` — seller mini-shop. Read: true; write: owner/admin.
- `tv_shop_items/{id}` — vitrine product/service (photoUrl, price, clipIds[], kind, district*, boostUntil, socialConsent, socialPostedAt, status). Read: true; create owner; update owner except boostUntil/socialPostedAt/viewCount/ownerPhone; admin any. Indexes: status+createdAt DESC; status+districtId+createdAt DESC; ownerPhone+createdAt DESC. Storage `tv_shop/{ownerPhone}/`.
- `users/{uid}/saved_tv_clips/{clipId}` — personal saves; owner read/create/delete. См. `mem:modules/tv_market`.
- `settings/app.platformDeliveryFeePercent` (number 0..100, default 5) — AVA дўкони етказиб бериш ҳақи % (маҳсулот жами × %).
- `platform_products.goodsKind` — `food` | `non_food` | '' (админ белгилайди).
- `platform_products/{id}` — Платформа дўкони каталоги (name, price, imageUrl, unit, totalStock 0=unlimited, soldToday, active, featuredOnHome, showInMarket, sortOrder). read:true; write:isAdmin. Index active+sortOrder. См. `mem:modules/platform_store`.
- `search_index/{id}` — глобал қидирув (types: service|platform_product|job|market_ad|intercity_route|bread_product|food_product|yuk_listing|local_place). Index: active + searchTokens CONTAINS. read:true; write:CF-only. См. `mem:modules/global_search`.
- `yuk_listings/{id}` — Шаҳарлараро load marketplace (cargo|truck). Fields: type, from, to, stops[], vehicleType, ownerId, ownerName, phone, status(active|closed), cargo/weight|capacity/freeSpace, price, comment, stars, createdAt, expiresAt(+48h), updatedAt, closedAt. Read: auth; create: owner+active; update: owner (no ownerId/createdAt/expiresAt/type/stars change) or admin; delete: admin. Indexes: status+createdAt DESC; status+expiresAt ASC. Auto-close via `expirePendingTrips`. **`isDemo: true`** = namoyish e'loni (`demo_yuk_1..7`, `expiresAt` ≈ +180 kun, `unit:'kg'`, phone = dispecher): kartada «Намойиш» pill, muddat hisoblagichi ko'rsatilmaydi. Seed/tozalash: `node functions/tools/seed_yuk_demo.js [--delete]`.
- `yuk_local_drivers/{autoId}` — Туман ичида эълонлар (2026-08 redesign). Doc auto-ID; egalik `ownerId` (canonical phone). Fields: ownerId, ownerName, phone, vehicleType, plateNumber, **capacityKg**, body L/W/H, acceptRadiusKm, loadStatus(empty|busy), lat, lng, locationLabel, **workStartMinutes/workEndMinutes** (0…1440), **expiresAt**, createdAt, updatedAt; rating/completedLoads qulflangan. **Online/heartbeat YO'Q.** Ko'rinish clientda: hasGps && !expired && inWorkHours. TTL create'da: birinchi ever → 60 kun, keyingi → 10 kun (`yuk_local_owner_meta/{ownerId}.everPublished`). Rules: auth read; create owner+lat/lng+work hours; update owner, notChanged(ownerId/rating/completedLoads/createdAt/expiresAt). Legacy `online`/`lastOnlineAt` o'qiladi lekin yozilmaydi. **`isDemo: true`** = namoyish (`demo_local_1..7`); client expire qilmaydi. Seed: `node functions/tools/seed_yuk_demo.js [--delete]`.
- `yuk_local_owner_meta/{ownerId}` — `everPublished: true` (TTL 60 vs 10). Read/write: owner phone match.
- `trips/{id}` local/alone: fare lock `toLat|toLng|lockedFare|lockedDistanceKm|lockedPolyline|fareLockedAt|fareLockVersion` (+estimatedPrice). Read: staff|passenger|driverPhone|`tripLocalOfferRead`. Update: passenger patch (status unchanged: radius/wallet); passenger cancel→cancelled+cancelledBy/At only; fare-lock; broadcast accept; driver release — **no client complete**; targeted accept/reject removed. Offer indexes: status+taxiType+geohash4+createdAt; status+taxiType+createdAt.
- `ads/{id}` — DUAL: jobs board (`work|service|ad`, status pending|active|completed|blocked; legacy `sell` ignored) + marketplace (`cheap_product`, **create status=pending** → admin `active|inactive`). Jobs board: create via submitJobAd; market create via submitMarketAd (ownerId/phone=canonical); owner patch + `isAdmin()` moderation; admin/owner delete via rules + admin CF. cheap_product: owner patch (+`searchTokens`); **`cheapProductAdminPatchOk` / `cheapProductAdminDeleteOk` for admin** + admin CF (`adminDeleteMarketAd` etc.). Market fields: `titleLower`, `searchTokens` (кирилл+лотин words from title/description). `complaints/{id}` — create CF-only `submitJobComplaint`; `reports` type `market_ad` CF-only `submitMarketComplaint`. Jobs+market ads create CF-only (client create false). settings/{docId} write isAdmin only. orders update: isAdmin or isStaff whitelist.
- `sell_submissions/{id}` — read: owner/admin/forwarded; create: auth+owner; **update/delete: false** (admin `adminUpdateSellSubmission` CF).
- `reports/{id}` — **dating_profile create CF-only** (`submitDatingReport`); boshqa turlar client create; read/update admin.

## Configuration-driven platform (geo + module config, 2026-07)
Config-driven module availability (Home dinamik). Kod: models `lib/models/geo_area.dart` (GeoRegion/GeoDistrict/ServiceArea), `lib/models/service_module_config.dart` (ModuleStatus enabled|coming_soon|hidden, ServiceModuleConfig.merge, kKnownModuleIds), repo `lib/repositories/service_config_repository.dart`, holder `lib/core/service_config_holder.dart` (bootstrap defaults+cache, applyServiceArea override, fallback=all enabled agar config yo'q → buzilmaslik kafolati).
Collections:
- `geo_regions/{id}`: name,nameUz,code,active,order. read:true write:isAdmin.
- `geo_districts/{id}`: regionId,name,nameUz,code,active,order. read:true write:isAdmin.
- `service_areas/{id}`: districtId,regionId(denorm),name,nameUz,type(mfy),active,order. read:true write:isAdmin.
- `service_area_modules/{id}`: serviceAreaId,districtId,regionId,modules{moduleId:{status}}. MFY override. read:true write:isAdmin.
- `config/module_defaults`: `enforce`(bool) + modules{moduleId:{status}} global baseline. Admin **Baseline** save: clears all area module overrides except `manualModules[]` (set only when admin picks non-Default on a district cell). Legacy seed (no manualModules) → fully cascades. Client `fetchServiceAreaModules` applies **only** manualModules keys (empty/missing → inherit Baseline; seed cannot override app). Holder area cache `svc_area_modules_v2` (legacy seed `svc_area_modules` purged); bootstrap clears area override then server-fetches defaults (keeps cache on fetch error — does not force enforce=false). `setServiceAreaModules` replaces `modules`+`manualModules` wholesale. enforce=true required for gating. Joriy holat (2026-07): intercity/sell/cheap_products_home/jobs/**local_taxi/marshrut**=enabled (butun viloyat); bread/food/courier/**carpet_wash/milk**=coming_soon (Gurlandan boshqa tumanlarda "tez orada"). Covered by existing `config/{docId}` rule (read:true write:isAdmin).
Resolution: effective = (defaults||fallbackAllEnabled) merge areaOverride. Availability FAQAT serviceAreaId; hisobot districtId (denormalize orders/trips tavsiya).
kKnownModuleIds (config+admin): bread,food,sell,cheap_products_home,marshrut,local_taxi,intercity,jobs,courier,**carpet_wash,milk**, **platform_store**, **tv_market**. Tire/car_wash config'da placeholder Home'da «tez orada»; **oil_change** ochiq (`OilChangeHomeScreen`). Oil: `users/{uid}/vehicles`(+oil_history), `oil_change_services`, `oil_change_bookings`, `settings/oil_change`, `car_profile_bonus_claims` (CF-only).
GATING KILL-SWITCH: `ServiceConfigHolder.enforced` = `config/module_defaults.enforce` (default false). enforce=false → statusOf() har doim enabled (regressiyasiz). Home `_openModule`+courier/jobs+**carpet_wash(onCarpetWash)/milk(onSut)** callbacklari `isOpenable()` bilan gate qilingan (enforce=false → o'zgarishsiz). Admin barcha user zonaga ega bo'lgach enforce=true qiladi.
users: qo'shiladi regionId/districtId/serviceAreaId/geoUpdatedAt (himoyalangan emas → owner yozadi; role/wallet himoyada).
ZONA MAJBURIY (2026-07): onboarding'da ServiceAreaPicker `showAreaDropdown:false` — foydalanuvchi FAQAT Viloyat→Tuman tanlaydi, tumanning birlamchi (eng past order) service_area avto-serviceAreaId bo'ladi (picker `_loadAreas` da). Onboarding validate case 5 + finish(): geoRegionId/geoDistrictId majburiy. Eski userlar: `lib/core/widgets/zone_gate.dart` ZoneGate main.dart'da HomeScreen'ni o'raydi — kesh/user-doc'da serviceAreaId bo'sh bo'lsa bloklovchi ZoneSelectScreen (Viloyat→Tuman, PopScope canPop:false). enforce'ga bog'liq EMAS — zona hozir yig'iladi, keyin admin enforce=true qiladi. Tarmoq xatosida bloklamaydi (Home ochiladi).
Seed skript: `functions/tools/seed-service-config.js` (firebase-admin, merge/idempotent, --dry). MUHIM: bu mashinada gRPC osiladi — `db.settings({preferRest:true})` + ishga tushirish `NODE_OPTIONS="--use-system-ca" node ...` (SSL local CA). Seed: xorazm region, 13 hudud (urganch_shahri,bogot,gurlan,qoshkopir,urganch,shovot,xiva_shahri,xiva,hazarasp,xonqa,yangiarik,yangibozor,tuproqqala — foydalanuvchi rasmiy ro'yxati), module_defaults, service_areas: svc_gurlan_markaz (zone, order 10) + **48 ta MFY** (`svc_gurlan_mfy_01`…`48` — `assets/data/mfy_list.json` Xorazm.Gurlan) + svc_{id} har boshqa hudud uchun (zone, local_taxi/marshrut enabled; bread/food/courier/carpet_wash/milk coming_soon). Gurlan MFY override = GURLAN_LOCAL_ENABLED. Admin matritsa: tuman bosilsa barcha MFY ustunlari ochiladi.
Status: fundament+rules(deployed ruleset 8a4d0ebf)+seed DONE. users geo fields+AddressEditScreen kaskad DONE. Home gating (enforce kill-switch) DONE. Admin 'Ҳудудлар ва хизматлар' ekrani DONE (`features/admin_web/screens/service_config_admin_screen.dart`: global baseline module status + enforce toggle; MFY override cascade). Onboarding kaskad DONE (`onboarding_controller.geoRegionId/geoDistrictId/geoServiceAreaId` + setGeoArea; finish() da saveServiceArea+applyGeo ixtiyoriy; `onboarding_screen._serviceAreaCard` manzil sahifasida). Denormalizatsiya DONE: `ServiceConfigHolder.reportStamp()` {regionId,districtId,serviceAreaId} → client-side trips(createSearch+marshrut), courier_orders(create), intercity_bookings(createBooking). Server-side CF `geoReportStamp(userData)` → placeOrderWithWallet, placeOrderPostPaid (orders bread/food), placeCarpetWashOrder, placeAgroPickupOrder. Holder: applyGeo() region/district'ni fetch'siz set qiladi + cache. Platforma refaktori TUGADI (deploy: firestore rules allaqachon; CF deploy KERAK).