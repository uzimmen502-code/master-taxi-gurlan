# Bugun (2026-07-13) — nasab chiziq: bus/stem + chekka

**DONE:**
1. Har oila: horizontal bus (`allocateBusY`) + vertikal o‘zak (`allocateStemX`)
2. Band segment register + route() da skip (T-tutashuv ruxsat)
3. Dinamik `laneStepFor` (16–28)
4. Chekkaga yopishish: past/yuqori markaz, `linePad` bo‘shlig‘i yo‘q
5. Bus Y — faqat karta koordinatalari (`+_pad` bilan mos)

Joy: `family_tree_line_router.dart`, `family_tree_view.dart` `_buildRoutedLines`.
Testlar: `family_tree_routing_test.dart` — o‘tdi.
